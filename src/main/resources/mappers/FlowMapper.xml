<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.metavm.flow.persistence.FlowMapper">
  <resultMap id="BaseResultMap" type="tech.metavm.flow.persistence.FlowPO">
    <!--@mbg.generated-->
    <!--@Table flow-->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="tenant_id" jdbcType="BIGINT" property="tenantId" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="type_id" jdbcType="BIGINT" property="typeId" />
    <result column="root_scope_id" jdbcType="BIGINT" property="rootScopeId"/>
  </resultMap>

  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    id, tenant_id, `name`, type_id, root_scope_id
  </sql>

  <select id="selectByIds" parameterType="java.lang.Long" resultMap="BaseResultMap">
    <!--@mbg.generated-->
    select
    <include refid="Base_Column_List" />
    from flow
    where id in
    <foreach collection="collection" item="id" separator="," open="(" close=")">
      #{id}
    </foreach>
    and deleted_at = 0
  </select>

  <update id="batchDelete">
    <!--@mbg.generated-->
    update flow
    set deleted_at = unix_timestamp() * 1000
    where id in
    <foreach collection="list" item="id" separator="," open="(" close=")">
      #{id}
    </foreach>
    and deleted_at = 0
  </update>

  <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id">
    <!--@mbg.generated-->
    insert into flow (id, tenant_id, `name`, type_id, root_scope_id)
    values
    <foreach collection="collection" item="item" separator=",">
      (#{item.id}, #{item.tenantId}, #{item.name},  #{item.typeId}, #{item.rootScopeId})
    </foreach>

  </insert>

  <update id="batchUpdate">
    <!--@mbg.generated-->
    <foreach collection="list" item="item" separator=";">
      update flow
      <set>
        <if test="item.tenantId != null">
          tenant_id = #{item.tenantId},
        </if>
        <if test="item.name != null">
          `name` = #{item.name},
        </if>
        <if test="item.typeId != null">
          type_id = #{item.typeId},
        </if>
      </set>
      where id = #{item.id} and deleted_at = 0
    </foreach>
  </update>

  <select id="query" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List"/>
    from
    flow
    where tenant_id = #{tenantId}
    and deleted_at = 0
    and type_id = #{typeId}
    <if test="name != null and name != ''">
      and name like concat('%', #{name}, '%')
    </if>
    limit #{start}, #{limit}
  </select>

  <select id="count" resultType="java.lang.Long">
    select
    count(1)
    from
    flow
    where
    tenant_id = #{tenantId}
    and deleted_at = 0
    and type_id = #{typeId}
    <if test="name != null and name != ''">
      and name like concat('%', #{name}, '%')
    </if>
  </select>

</mapper>