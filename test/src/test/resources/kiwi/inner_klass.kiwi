
class Product(quantity: int) {

    priv var inventory = Inventory(quantity)

    fn getInventory() -> Inventory {
        return inventory
    }

    class Inventory(priv var quantity: int) {

        fn getQuantity() -> int {
            return quantity
        }

    }

}