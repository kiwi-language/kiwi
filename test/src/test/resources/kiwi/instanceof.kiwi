package org.metavm.test

import org.metavm.object.type.Klass

class InstanceOfFoo {

    func isInstance(o: any) -> boolean {
        return o is InstanceOfFoo
    }

    func isKlassInstance(o: any) -> boolean {
        return o is Klass
    }

}