package org.metavm.test;

import org.metavm.object.type.Klass;

class InstanceOfFoo {

    public boolean isInstance(any o) {
        return o instanceof InstanceOfFoo;
    }

    public boolean isKlassInstance(any o) {
        return o instanceof Klass;
    }

}