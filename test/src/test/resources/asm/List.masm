import java.util.List;
import java.util.ArrayList;
import org.metavm.api.ChildList;

struct Node<T> {
    private string label;
    private T value;

    public Node(string label, T value) {
        this.label = label;
        this.value = value;
    }

    public string getLabel() {
        return this.label;
    }

    public T getValue() {
        return this.value;
    }

}

struct MyList<T> {

    private string label;
    private readonly child ChildList<Node<T>> nodes;

    public MyList(string label, List<Node<T>> nodes) {
        this.label = label;
        nodesCopy: new ChildList<Node<T>>(nodes);
        this.nodes = nodesCopy;
    }

    public string getLabel() {
        return this.label;
    }

    public List<Node<T>> getNodes() {
        nodes: unew ArrayList<Node<T>>(this.nodes);
        return nodes;
    }

}