package org.metavm.autograph;

import lombok.extern.slf4j.Slf4j;
import org.metavm.util.TestConstants;

@Slf4j
public class StdCompilingTest extends CompilerTestBase {

    public static final String STD_SOURCE_ROOT = "/Users/leen/workspace/object/lab/src/main/std2";

    public void test() {
//        compile(STD_SOURCE_ROOT);
//        submit(() -> {
//            var mapKlassId = getClassTypeByCode("org.metavm.util2.Map").id();
//            try(var context = entityContextFactory.newContext(TestConstants.APP_ID)) {
//                var mapKlass = context.getKlass(mapKlassId);
//                var method = mapKlass.getMethodByCode("getOrDefault");
////                Assert.assertTrue(method.isRootScopePresent());
//                log.debug("{}", method.getText());
//            }
//        });
    }

}
