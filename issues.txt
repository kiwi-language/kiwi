1 Type narrowing in flow doesn't cover the following case:

    public static <T> T findRequired(Iterable<? extends T> iterable, Predicate<? super T> predicate) {
        T t = find(iterable, predicate);
        if (t == null) {
            throw new IllegalArgumentException("Not found");
        }
        return t;
    }

2 Simplify the process of adding standard exception types