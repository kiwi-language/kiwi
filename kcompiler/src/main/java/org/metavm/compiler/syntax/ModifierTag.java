package org.metavm.compiler.syntax;

public enum ModifierTag {
    PUBLIC,
    PRIVATE,
    PROTECTED,
    STATIC,
    READONLY,
    TRANSIENT,
    INTERFACE,
    CLASS,
    ENUM,
    DELETED,
    ABSTRACT,
    VALUE,
}
